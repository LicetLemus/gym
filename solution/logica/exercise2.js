// write a program that calculates the average of a student in base a 3 notes
// different, entered by the user.

function average(note1, note2, note3) {
    return (note1 + note2 + note3)/3
}

console.log(average(5, 4.5, 2))

