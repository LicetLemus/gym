// write a program that requests two numbers the user and displays in the console the result
// of the addition, rest, multiplication and division between both numbers.
const num1 = 10;
const num2 = 20;
const result = (num1 ,num2) =>  [num1 + num2, num1 - num2, num1 * num2, num1 / num2]
console.log(result(num1, num2))